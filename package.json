{
  "name": "PastStats",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "cd database && yarn setup && cd ../server && yarn setup && cd ../web && yarn setup && cd ../mobile && yarn setup",
    "dev:mobile": "cd mobile && yarn ios",
    "dev:web": "cd web && yarn start",
    "database": "cd database && yarn start",
    "server": "cd server && yarn start",
    "update-env": "cp .env ./database && cp .env ./server && cp .env ./web && cp .env ./mobile"
  },
  "keywords": [],
  "author": "DillonDow",
  "license": "ISC",
  "dependencies": {
  }
}
