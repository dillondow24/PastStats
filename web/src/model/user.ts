/**
 * User model
 *
 * @export
 * @interface User
 */
export interface User {
    uid: string;
    timestamp: number;
    username: string;
    email: string;
    phone: string; 
    avatar: string;
}